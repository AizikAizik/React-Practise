import React from 'react'

const Person = ({ person }) => {
        return (
                <div>
                        <h1>Hello {person.name}</h1>
                </div>
        )
}

export default Person 